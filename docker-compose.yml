version: '3.8'

services:
  # Character Builder Frontend (Production)
  character-builder:
    build: ./frontend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

volumes:
  app_data: